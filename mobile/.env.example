# Base URL of the FastAPI backend.
# If testing on a physical phone via Expo Go, use your computer's LAN IP,
# NOT localhost (the phone can't reach your laptop's localhost over Wi-Fi).
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
