# This root file just documents where each component's env config lives.
# Copy each of the following individually:
#   backend/.env.example -> backend/.env
#   web/.env.example     -> web/.env
#   mobile/.env.example  -> mobile/.env
#
# docker-compose.yml sets its own environment variables directly and does
# not read these .env files.
